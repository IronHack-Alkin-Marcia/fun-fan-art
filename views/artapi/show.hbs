<section class="features main-bg" id="features">
  <div class="container">
    <div class="section-heading text-center">
      <h2 class="pt-5">{{art.title}}</h2>
      <p><i>{{art.smallDescription}}</i></p>
    </div>
    <div class="row">
      <div class="col-lg-12 my-auto">
        <div class="container-fluid">


          <div class="row">


            <div class="card card-show box-shadow" style="width: 700px">
              <img class="card-img-top" src="{{art.img}}" alt="{{art.title}}">
              <div class="card-body">
                <h3 class="card-title">{{art.title}}</h3>
                <h5 class="card-title"><span class="pretty">Artist:</span> {{art.artist}}</h5>
                <p class="card-text"><span class="pretty">Description:</span> {{art.description}}</p>
                <p class="card-text"><span class="pretty">Year:</span> {{art.year}}</p>
                <div class="show-btn">
                  <a href="/artapi/" class="btn btn-bg"><i>Go Back</i></a>
                  <a href="/saveart/{{art.artId}}" class="btn btn-bg btn-bm"><i>Bookmark <i
                        class="fas fa-heart"></i></i></a>
                </div>
              </div>
            </div>


          </div>

        </div>
      </div>
    </div>
  </div>
</section>